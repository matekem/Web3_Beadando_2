<!-- Example content for Admin/Index.cshtml -->

@{
    ViewData["Title"] = "Admin Panel";
}

<!-- Button to add a new subject with instructor -->
<a class="btn btn-primary" href="@Url.Action("NewSubject", "Admin", new { area = "" })">Add New Subject</a>

<!-- Button to add a new classroom -->
<a class="btn btn-primary" href="@Url.Action("NewClassroom", "Admin", new { area = "" })">Add New Classroom</a>

<!-- Button to set class schedule -->
<a class="btn btn-primary" href="@Url.Action("EditSubject", "Admin", new { area = "" })">Set Class Schedule</a>

<!-- Button to add homework or exam -->
<a class="btn btn-primary" href="@Url.Action("Assignments", "Admin", new { area = "" })">Assignments</a>