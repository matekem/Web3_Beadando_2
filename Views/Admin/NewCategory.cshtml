@using Web3_Beadando.Services
@model Web3_Beadando.Services.SchoolService
@{
    ViewBag.Title = "New Category";
}

<h1>New Category</h1>
<form method="post">
    <div class="form-group">
        <label for="CategoryName">Category Name:</label>
        <input type="text" class="form-control" id="CategoryName" name="CategoryName" required />
    </div>
    <input type="submit" />
</form>

<!-- List all the categories in the db -->

<table class="table">
    <thead>
        <tr>
            <th>Category Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        
        @{
            SchoolService schoolService = new();
            foreach (var category in schoolService.categories)
        {
            <tr>
                <td>@category.Name</td>
                <td>
                    <form method="post" asp-action="DeleteCategory" asp-route-id="@category.Id">
                        <button type="submit" class="btn btn-primary">Delete</button>
                    </form>
                </td>
            </tr>
        }}
    </tbody>

